include<stdio.h>

int;
double a, b, c, d, i;

int(main){
  //式を決定
  printf("式を決定してください。");
  scanf("%lf\n", &y)
  //範囲を決定
  printf("範囲　x0 ~xn　を決定してください。\n");
  printf("x0 = ");
  scanf("%lf\n", &a);
  printf("xn = ");
  scanf(%lf\n", %b);
 //Δxを決定
  printf("Δx　を決定してください。\n");
  printf("Δx = ");
  scanf("%lf\n", &c);
  //計算回数を決定
  d = (b - a + 1)/c;
  //計算
}
  
